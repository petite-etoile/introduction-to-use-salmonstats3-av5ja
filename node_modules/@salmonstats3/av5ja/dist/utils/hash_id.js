import { WeaponInfoMain } from '../utils/weapon_info_main';
/**
 * IDからハッシュを計算する
 * @param id
 * @returns
 */
export function hash(id) {
    return WeaponInfoMain.Hash[WeaponInfoMain.Id[id]];
}
/**
 * HashからIDを計算する
 * @param hash
 * @returns
 */
export function id(hash) {
    return WeaponInfoMain.Id[Object.entries(WeaponInfoMain.Hash).find(([, value]) => value === hash)?.[0] ?? WeaponInfoMain.Hash.Dummy];
}
//# sourceMappingURL=hash_id.js.map