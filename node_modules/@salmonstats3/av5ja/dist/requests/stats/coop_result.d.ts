import { Method } from '../../enum/method';
import { RequestType, Headers, Parameters, ResponseType } from '../../utils/request';
import 'reflect-metadata';
export declare namespace CoopResult {
    class Request implements RequestType {
        readonly baseURL: string;
        readonly headers: Headers;
        readonly method: Method;
        readonly parameters: Parameters;
        readonly path: string;
        constructor(results: any[]);
        request(response: any): CoopResult.Response[];
    }
    class Response implements ResponseType {
        result_id: string;
        schedule_id: string;
    }
}
export declare function set_coop_history_details(results: any[]): Promise<CoopResult.Response[]>;
