import { Method } from '../../enum/method';
import { RequestType, Headers, Parameters } from '../../utils/request';
import 'reflect-metadata';
export declare namespace CoopSchedule {
    class Request implements RequestType {
        readonly baseURL: string;
        readonly headers: Headers;
        readonly method: Method;
        readonly parameters: Parameters;
        readonly path: string;
        constructor(results: any);
        request(response: any): void;
    }
}
export declare function set_coop_schedules(schedule: any): Promise<void>;
