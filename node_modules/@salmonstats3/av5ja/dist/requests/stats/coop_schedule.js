import { Method } from '../../enum/method';
import { base_url } from '../../utils/env';
import { request } from '../../utils/request';
import 'reflect-metadata';
export var CoopSchedule;
(function (CoopSchedule) {
    class Request {
        baseURL = base_url;
        headers = {
            'Content-Type': 'application/json',
        };
        method = Method.POST;
        parameters;
        path = 'v1/schedules';
        constructor(results) {
            this.parameters = JSON.parse(JSON.stringify(results));
        }
        // eslint-disable-next-line @typescript-eslint/no-unused-vars
        request(response) {
            return;
        }
    }
    CoopSchedule.Request = Request;
})(CoopSchedule || (CoopSchedule = {}));
export async function set_coop_schedules(schedule) {
    return await request(new CoopSchedule.Request(schedule));
}
//# sourceMappingURL=coop_schedule.js.map